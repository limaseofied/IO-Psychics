foreach($signs as $sign){

$urls = [
"daily/$sign",
"daily/previous/$sign",
"daily/next/$sign"
];

foreach($urls as $url){

$response = Http::withBasicAuth($user,$pass)
->get("https://json.astrologyapi.com/v1/sun_sign_prediction/".$url);

$data = $response->json();

DailyHoroscope::updateOrCreate(
[
'sun_sign'=>$data['sun_sign'],
'prediction_date'=>Carbon::createFromFormat('d-m-Y',$data['prediction_date'])->format('Y-m-d')
],
[
'personal_life'=>$data['prediction']['personal_life'],
'profession'=>$data['prediction']['profession'],
'health'=>$data['prediction']['health'],
'emotions'=>$data['prediction']['emotions'],
'travel'=>$data['prediction']['travel'],
'luck'=>$data['prediction']['luck']
]
);

}

}

------------ db details ---------

u912758906_IO_Psychics

IO_Psychics_2026

--------------

master table list:

specialities/topic
tools 
skill
reading style 
guides 

subscription plan (Discovery/30 min/ core guide/80$ , Quest/60 min/ senior duide/140$, Odyssey/90min/master guide/200$)
pay per session plan (15min/50$, 30min/90$, 45min/130$, 60min/169$)

if user subscribed a plan and midway he can upgrade to higher plan



------------

ALTER TABLE `reading_styles` ADD `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `name`, ADD `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `created_at`;

------------ To do ---------------
subscription plan master 
pay per session master
Guide master


--------------------------------


SHOW CREATE TABLE guides;

-------------------------------------
CREATE TABLE `guides` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(150) NOT NULL,
  `email` varchar(150) DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `password` varchar(255) DEFAULT NULL,
  `address` text DEFAULT NULL,
  `experience` int(11) DEFAULT 0,
  `rating` decimal(2,1) DEFAULT 0.0,
  `guide_level` enum('core','senior','master') DEFAULT 'core',
  `price_per_min` decimal(10,2) DEFAULT 0.00,
  `speciality_id` int(11) DEFAULT NULL,
  `tool_id` int(11) DEFAULT NULL,
  `skill_id` int(11) DEFAULT NULL,
  `reading_style_id` int(11) DEFAULT NULL,
  `status` tinyint(4) DEFAULT 1,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`),
  KEY `speciality_id` (`speciality_id`),
  KEY `tool_id` (`tool_id`),
  KEY `skill_id` (`skill_id`),
  KEY `reading_style_id` (`reading_style_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci


<a href="{{ route('rewards') }}">IO Rewards</a>
<a href="{{ route('plans') }}">Plans & Pricing</a>
<a href="{{ route('signup') }}">Sign Up</a>
<a href="{{ route('login') }}">Log In</a>
<a href="{{ route('guides') }}">Guides</a>
<a href="{{ route('horoscopes') }}">Horoscopes</a>
<a href="{{ route('tarot') }}">Tarot</a>
<a href="{{ route('connect.tarot') }}">Connect With Tarot Guides</a>
<a href="{{ route('topics') }}">Topics</a>
<a href="{{ route('how.it.works') }}">How It Works</a>
<a href="{{ route('about') }}">About IO</a>
<a href="{{ route('contact') }}">Contact</a>
<a href="{{ route('free.trial') }}" class="primary-btn1">Unlock Your Free Trial</a>



------ to do admin panel ------

Horoscope daily list done
Horoscope Monthly List  done

Testimonials master

Services master (Aromatherapy , Candle Energy,Connecting with Your Spirit Guides,The Art of Tarot)
--------------------------
title 
thumbnail 
banner image
small description  (editor)
Full Description  (editor)
Steps (Add More)
-----------
image , title , content (editor)

Final thoughts (editor)

------ to do website ------
Sign up page in frontend
Subscription plan choose by user
currently online guides (home page )