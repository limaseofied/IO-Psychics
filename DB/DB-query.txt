------ alter query 19-march-2026 --------

alter table pay_per_session_plans add `guide_level` enum('core','senior','master') DEFAULT 'core';
ALTER TABLE `faqs` ADD `display_in_home` INT NOT NULL DEFAULT '0' AFTER `category`;

CREATE TABLE `testimonials` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `content` text  NULL,  
  `status` enum('active','inactive') DEFAULT 'inactive',
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;


CREATE TABLE `services` (
  `id` INT(11) NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(255) NOT NULL,
  `thumbnail` VARCHAR(255) DEFAULT NULL,
  `banner_image` VARCHAR(255) DEFAULT NULL,
  `small_description` TEXT NULL,
  `full_description` LONGTEXT NULL,
  `final_thoughts` LONGTEXT NULL,
  `created_at` TIMESTAMP NULL DEFAULT NULL,
  `updated_at` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `service_steps` (
  `id` INT(11) NOT NULL AUTO_INCREMENT,
  `service_id` INT(11) NOT NULL,
  `image` VARCHAR(255) DEFAULT NULL,
  `title` VARCHAR(255) DEFAULT NULL,
  `content` TEXT NULL,
  `created_at` TIMESTAMP NULL DEFAULT NULL,
  `updated_at` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  FOREIGN KEY (`service_id`) REFERENCES `services`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

--------------------- 20 march 2026 --------

ALTER TABLE `guides` CHANGE `price_per_min` `price_per_session` DECIMAL(10,2) NULL DEFAULT '0.00';

ALTER TABLE `guides` ADD `profile_image` VARCHAR(255) NULL DEFAULT NULL AFTER `phone`;

ALTER TABLE `guides` ADD `display_in_home` INT NOT NULL DEFAULT '0' AFTER `updated_at`;


--------------- 20 march ------

ALTER TABLE guides  ADD COLUMN is_online TINYINT(1) DEFAULT 0;
ALTER TABLE guides  ADD COLUMN last_seen DATETIME DEFAULT NULL;
ALTER TABLE horoscope_signs ADD COLUMN traits TEXT,ADD COLUMN personality TEXT;
ALTER TABLE `specialities` ADD `slug` VARCHAR(255) NULL DEFAULT NULL AFTER `name`, ADD `image` VARCHAR(255) NULL DEFAULT NULL AFTER `slug`;

CREATE TABLE contacts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_type VARCHAR(50),
    subject VARCHAR(150),
    name VARCHAR(150),
    email VARCHAR(150) NOT NULL,
    message TEXT NOT NULL,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

--------------------
SELECT *,
  CASE 
    WHEN last_seen >= NOW() - INTERVAL 2 MINUTE THEN 1 
    ELSE 0 
  END AS is_online
FROM guides;

1. When guide logs in

As soon as login is successful:

UPDATE guides 
SET is_online = 1,
    last_seen = NOW()
WHERE id = ?;

. When guide logs out
UPDATE guides 
SET is_online = 0,
    last_seen = NOW()
WHERE id = ?;

